.claude

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
build/

# Secrets & runtime artifacts — NEVER commit (CODING_STANDARDS §4.1)
keys/
var/
*.pem
*.key
.env

# F2 landmark predictor weights (~99 MB) — fetched on demand via
# scripts/fetch_landmarks.py, never committed.
models/

# Out-of-scope Figma frontend bundle — not part of the Python prototype
# (CODING_STANDARDS §0 Scope Lock; FastAPI deferred, CLI-only this round).
# Files stay on disk; remove these lines and re-add if frontend work is
# reintroduced via a discussed milestone.
ATTRIBUTIONS.md
BORDER_BEAM_INTEGRATION.md
INTEGRATION_SUMMARY.md
default_shadcn_theme.css
index.html
package.json
pnpm-workspace.yaml
postcss.config.mjs
tsconfig.json
tsconfig.node.json
vite.config.ts
src/
guidelines/
